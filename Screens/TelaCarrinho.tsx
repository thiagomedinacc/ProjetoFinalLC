import {Text} from 'react-native';
import { RootStackScreenProps } from '../types';

export default function TelaCarrinho({navigation, route} : RootStackScreenProps<'TelaCarrinho'>) {
  return <Text> TELA CARRINHO</Text>;
}
